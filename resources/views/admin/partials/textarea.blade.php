<div class="form-group">
    <label for="{{ @$name }}">{{ @$label }}</label>
    <textarea placeholder="{{ @$placeholder }}" class="form-control {{ @$class }}" name="{{ @$name }}"  >{{ @${@$name} }}</textarea>
</div>